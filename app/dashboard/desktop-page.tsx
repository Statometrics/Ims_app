export default function DesktopPage() {
  return (
    <div
      style={{
        backgroundColor: "#000",
        color: "#0ff",
        minHeight: "100vh",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontSize: "2rem",
      }}
    >
      DESKTOP PAGE TEST LOADED
    </div>
  );
}
